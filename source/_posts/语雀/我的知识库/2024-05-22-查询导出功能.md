
    layout: post
    title: 查询导出功能
    tags:
    - 语雀
    categories:
    - [语雀,我的知识库]
    abbrlink: 
    date: 2024-05-22 17:28:47
    
query.js
```bash
db.getCollection('GeoFile.chunks').aggregate([
   {
     $lookup:
       {
         from: "GeoFile.files",
         localField: "files_id",
         foreignField: "_id",
<!--more-->
         as: "inventory_docs"
       }
  },{
        '$group': {
            '_id': {
                _id: '$_id',
                files_id: '$files_id',
                n: '$n',
                chunkSize: '$inventory_docs.chunkSize',
                uploadDate: '$inventory_docs.uploadDate'
                
            },
             num_tutorial: {
                $sum: 1
            }
        }
    }
]).forEach(
    function(document) {
       print(document._id._id+"," + document._id.files_id+"," + document._id.n+"," + document._id.chunkSize+"," + document._id.uploadDate +"," + document.num_tutorial);
    }
)
```
执行命令
mongo.exe 127.0.0.1:28012/filewarehouse --authenticationDatabase filewarehouse -u admin -p filewarehouse --quiet  E:/Resource/MongoDB/query.js > E:/Resource/MongoDB/result.csv


