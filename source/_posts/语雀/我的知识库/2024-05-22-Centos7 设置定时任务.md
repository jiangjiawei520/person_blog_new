
    layout: post
    title: Centos7 设置定时任务
    tags:
    - 语雀
    categories:
    - [语雀,我的知识库]
    abbrlink: 
    date: 2024-05-22 17:28:46
    
## 1、安装 crontabs服务并设置开机自启

> # 安装定时任务
> yum install crontabs
> 
> # 设置开机启动
> $ systemctl enable crond
> 
> # 启动定时任务
<!--more-->
> $ systemctl start crond

## 2、配置定时规则
在 /etc/crontab 文件中添加 */1 * * * * /root/crontab.sh 备份脚本路径
> --每分钟执行一次
> */1 * * * * root /usr/local/tomcat/crontab.sh
> --每1小时执行
> 0 */1 * * *  /usr/local/tomcat/crontab.sh

添加定时任务：
> $ crontab -e
> 0 */1 * * *  /usr/local/tomcat/crontab.sh

查看所有定时任务
> [root@network ~]# crontab -l
> SHELL=/bin/bash
> PATH=/sbin:/bin:/usr/sbin:/usr/bin
> MAILTO=root
> 
> # For details see man 4 crontabs
> 
> # Example of job definition:
> # .---------------- minute (0 - 59)
> # |  .------------- hour (0 - 23)
> # |  |  .---------- day of month (1 - 31)
> # |  |  |  .------- month (1 - 12) OR jan,feb,mar,apr ...
> # |  |  |  |  .---- day of week (0 - 6) (Sunday=0 or 7) OR sun,mon,tue,wed,thu,fri,sat
> # |  |  |  |  |
> # *  *  *  *  * user-name  command to be executed
> 0 */1 * * *  /usr/local/tomcat/crontab.sh

保存生效
> systemctl reload crond

## 3、编写需要执行的脚本
> vim crontab.sh
> #! /bin/bash
> echo 1 > /proc/sys/vm/drop_caches
> echo 2 > /proc/sys/vm/drop_caches
> echo 3 > /proc/sys/vm/drop_caches
> echo `date` 执行清理缓存 `cat /proc/meminfo| grep MemFree` >> /usr/local/tomcat/crontab.log

## 4、定时规则
![image.png](https://cdn.nlark.com/yuque/0/2021/png/12484160/1638149722782-6c6b85da-f990-451f-b9fb-7d63e962b99f.png#clientId=uc357c1bb-5c2c-4&from=paste&height=203&id=ubff6b05b&originHeight=406&originWidth=847&originalType=binary&ratio=1&size=411741&status=done&style=none&taskId=u84ba8b7b-e2dd-4381-83c6-52dee6a4dac&width=423.5)
规则很简单，看注释就能看懂了，从左到右分别是 分钟（0~59）、小时（0~23）、天（1~31）、月(1~12)、星期(0~6)、用户名、要执行的命令或者脚本。

脚本内容如下：

#! /bin/bash
> echo `date` > /root/crontab.log

每分钟将时间打印到crontab.log日志文件

3、保存生效
> $ systemctl reload crond

4、查看任务
> $ crontab -l

