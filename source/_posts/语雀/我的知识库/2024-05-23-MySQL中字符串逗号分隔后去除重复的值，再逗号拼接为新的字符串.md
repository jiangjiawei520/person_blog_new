---
ayout: post
title: MySQL中字符串逗号分隔后去除重复的值，再逗号拼接为新的字符串
tags:
  - 语雀
categories:
  - - 语雀
    - 我的知识库
abbrlink: 215d12f3
password: Grbk@2024
date: 2024-05-23 16:24:15
---
# 目标效果
原字符串值：101,102,103,102
处理后的值：101,102,103

# 创建一个函数
```
CREATE FUNCTION remove_duplicate_values(val VARCHAR(4000)) 
RETURNS VARCHAR(4000)
<!--more-->
BEGIN
    DECLARE result VARCHAR(4000) DEFAULT '';
    DECLARE arr VARCHAR(4000) DEFAULT '';
    DECLARE i INT DEFAULT 0;
    DECLARE val_len INT DEFAULT 0;
    DECLARE sub_val VARCHAR(4000);

    SET arr = replace(val, ',', ',');
    SET val_len = CHAR_LENGTH(arr);

    WHILE i < val_len DO
        SET sub_val = SUBSTRING_INDEX(SUBSTRING_INDEX(arr, ',', i + 1), ',', -1);
        IF NOT FIND_IN_SET(sub_val, result) THEN
            SET result = CONCAT(result, sub_val, ',');
        END IF;
        SET i = i + 1;
    END WHILE;
    SET result = TRIM(TRAILING ',' FROM result);
    RETURN result;
END;

```

# 调用函数测试
SELECT remove_duplicate_values(‘字符串值’);

# 结合SQL
Update table_name SET Field_Name=remove_duplicate_values(Field_Name) WHERE …

