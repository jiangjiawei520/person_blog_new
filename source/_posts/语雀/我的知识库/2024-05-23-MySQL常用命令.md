---
ayout: post
title: MySQL常用命令
tags:
  - 语雀
categories:
  - - 语雀
    - 我的知识库
abbrlink: 92cd9797
password: Grbk@2024
date: 2024-05-23 16:24:15
---
## 配置

### 查找修改事务自动提交开关
```
mysql> select @@autocommit; 　　--查看autocommit配置
mysql> set autocommit=1;　　　　 --设置参数[0,1]
```

<!--more-->
### 查看隔离级别
`mysql> select @@tx_isolation;`

### 查看当前引擎的状态信息
`mysql> show engine innodb status;`

### 查看及修改配置
```
innodb_lock_wait_timeout  InnoDB的锁超时等待参数
mysql>SHOW GLOBAL VARIABLES LIKE 'innodb_lock_wait_timeout';
mysql>SET  GLOBAL innodb_lock_wait_timeout=100;
```

### 查询数据库支持的存储引擎
`mysql>show engines;`

### 查看正在运行的线程
```
mysql> show processlist;
mysql> show full processlist;
mysql -e "show full processlist">tmp/proc.txt
kill pid
```

## 事务语句

### 显式开启一个事务
`begin或者start transaction`

### 提交一个事务，使修改是永久的
`commit`

### 回滚事务，撤销未提交的修改
`rollback`

### 创建一个保存点，一个事务中可以有多个保存点
`savepoint identifier`

### 删除一个事务的保存点，若没有这个保存点，抛出异常
`release savepoint identifier`

### 事务回滚到保存点
`rollback to identifier：`

### 设置事务的隔离级别，InnoDB提供的级别有read uncommitted，read committed，repeatable read，serializable
`set transaction：`

## 常用查询数据库信息命令
下面的tb_me为表名，db_me为数据库名

### 查看所有数据库
`mysql> show databases;`

### 使用这个数据库
`mysql> use db_me;`

### 显示所有表名
`mysql> show tables;`

### 简单描述表结构，字段类型
`mysql> desc tb_me;`

### 查看列名和备注
`mysql> select column_name, column_comment from information_schema.columns where table_schema =(select database()) and table_name = 'tb_me';`

### 查看表的注释
`mysql> select table_name,table_comment from information_schema.tables where table_schema = (select database()) and table_name ='tb_me'`

### 查看表生成的DDL
`mysql> show create table tb_me;` `mysql> show create table tb_me\G;`

### 查看mysql sources 的输出
`tee output.log` `mysql> source aa.sql`

### 连接数据库
mysql命令用户连接数据库。
`mysql命令格式：mysql -h主机地址-u用户名－p用户密码`

### 退出MYSQL命令
`mysql> exit`

## mysql权限设置

### 修改用户密码
mysqladmin命令用于修改用户密码。 `mysqladmin命令格式：mysqladmin -u 用户名-p 旧密码password 新密码`

1. 给root加个密码ab12

`mysqladmin -u root -password ab12`
注：因为开始时root没有密码，所以-p 旧密码一项就可以省略了。 2. 再将root的密码改为djg345
`mysqladmin -u root -p ab12 password djg345`

3. grant on：新增用户 grant on命令用于增加新用户并控制其权限。 grant on命令格式：

`grant select on 数据库.* to 用户名@登录主机identified by “密码”;`

- 增加一个用户test1，密码为abc，让他可以在任何主机上登录，并对所有数据库有查询、插入、修改、删除的权限。首先用root用户连入MYSQL，然后键入以下命令：

`grant select,insert,update,delete on *.* to [email=test1@”%]test1@”%[/email]” Identified by “abc”;`

- 增加一个用户test2密码为abc，让他只可以在localhost上登录，并可以对数据库mydb进行查询、插入、修改、删除的操作（localhost指本地主机，即MYSQL数据库所在的那台主机），这样用户即使用知道test2的密码，他也无法从internet上直接访问数据库，只能通过MYSQL主机上的web页来访问了。

`grant select,insert,update,delete on mydb.* to [email=test2@localhost]test2@localhost[/email] identified by “abc”;`
如果你不想test2有密码，可以再打一个命令将密码消掉。
`grant select,insert,update,delete on mydb.* to [email=test2@localhost]test2@localhost[/email] identified by “”;`
**常用命令：** `GRANT ALL PRIVILEGES ON *.* TO 'gameboys'@'%' IDENTIFIED BY 'gameboys1234';` `GRANT ALL PRIVILEGES ON *.* TO 'gameboys'@'127.0.0.1' IDENTIFIED BY 'gameboys1234';`

## 创建数据库和表

### 刷新配置，这个在直接sql修改数据表的情况下需要使用，grant不需要
`FLUSH PRIVILEGES;`

### 创建数据库
create命令格式：create database <数据库名>; 建立一个名为xhkdb的数据库：
`mysql> create database xhkdb;`

### 显示数据库
`mysql> show databases;`

### 删除数据库
```
mysql> drop database xhkdb;
mysql> drop database if exists drop_database;
```

### 使用数据库
`mysql> use xhkdb;`

### 当前连接的数据库
`mysql> select database();`

### 显示MYSQL的版本
`mysql> select version();`

### 显示当前时间
`mysql> select now();`

### 显示年月日
```
mysql> SELECT DAYOFMONTH(CURRENT_DATE); 
mysql> SELECT MONTH(CURRENT_DATE);
```

### 显示字符串
`mysql> SELECT "welecome to my blog!";`

### 当计算器用
`mysql> select ((4 * 4) / 10 ) + 25;`

## 增删改数据表

### 创建数据表
创建员工表tb_emp1，结构如下表所示。

| 字段名称 | 数据类型 | 备注 |
| --- | --- | --- |
| id | INT(11) | 员工编号 |
| name | VARCHAR(25) | 员工名称 |
| depId | INT(11) | 所在部门编号 |
| salary | FLOAT | 工资 |

```
mysql> CREATEDATABASE test_db;
mysql> USE test_db;
mysql> create table tb_emp1
    (
    id int(11),
    name varchar(25),
    deptId int(11),   
    salary float
);
mysql> show tables;
```

### 获取表结构
```
mysql> desc tb_emp1;
mysql> show create table tb_emp1\G;
如果不加’\G’参数，显示的结果可能非常混乱，加上参数’\G’之后，可使显示结果更加直观，易于查看。
```

### 删除数据表
`mysql> drop table if exists tb_dept2;`

### 增加字段
```
mysql> ALTER TABLE tb_dept1 ADD managerId INT(10);
mysql> alter table user add COLUMN new1 VARCHAR(20) DEFAULT NULL; //增加一个字段，默认为空
mysql> alter table user add COLUMN new2 VARCHAR(20) NOT NULL;  //增加一个字段，默认不能为空
```

### 删除一个字段
`mysql>alter table user DROP COLUMN new2; 　 //删除一个字段`

### 修改一个字段
```
mysql> alter table user MODIFY new1 VARCHAR(10); 　//修改一个字段的类型
mysql> alter table user CHANGE new1 new4 int;　 //修改一个字段的名称，此时一定要重新指定该字段的类型
```

### 修改表名
`ALTERTABLE tb_dept3 raname tb_deptment3;`

## 增删改查数据

### 向表中插入数据
```
mysql> INSERT INTO 表名称VALUES (值1, 值2,....) 
mysql> INSERT INTO table_name (列1, 列2,...) VALUES (值1, 值2,....)
```

### 查询表中数据
MySQL从数据表中查询数据的基本语句为SELECT语句。SELECT语句的基本格式是：
```
SELECT 
{* | <字段列表>} 
[ 
FROM <表1>,<表2>... 
[WHERE <表达式> 
[GROUP BY <group by definition>] 
[HAVING <expression> [{<operator> <expression>}...]] 
[ORDER BY <order by definition>] 
[LIMIT [<offset>,] <row count>] 
] 
SELECT [字段1,字段2,…,字段n] 
FROM [表或视图] 
WHERE [查询条件];
```

### 删除记录
`mysql> DELETE FROM person WHERE id = 11;`

### 修改表中的数据
```
mysql> update person set age = 15, name=’LiMing’  WHERE id = 11;
mysql> update user set age=age+1 where id=1;
```

### 插入或者更新数据
主键为id
`mysql> INSERT INTO t1 (id,b,c) VALUES (1,2,3) ON DUPLICATE KEY UPDATE c=c+1;`

## 备份还原数据库

### 备份数据库
```
mysqldump  –u user –h host –ppassword dbname[tbname,[tbname...]]> filename.sql
mysqldump -u root -p booksdb > C:/backup/booksdb_20130301.sql
```

1. 导出结构不导出数据

`mysqldump --opt -d 数据库名 -h127.0.0.1 -uroot　-proot　>　xxx.sql`

2. 导出数据不导出结构

`mysqldump　-t　数据库名 -h127.0.0.1 　-uroot　-p　>　xxx.sql`

3. 导出数据和表结构

`mysqldump　数据库名 -h127.0.0.1 　-uroot　-p　>　xxx.sql`

4. 导出特定表的结构

`mysqldump　-uroot　-p　-B　数据库名 -h127.0.0.1 　--table　表名　>　xxx.sql`

### mysqldump常用参数说明
| 参数 | 含义 |
| --- | --- |
| --all-databases, -A | 导出全部数据库。 |
| --all-tablespaces, -Y | 导出全部表空间。 |
| --no-tablespaces, -y | 不导出任何表空间信息。 |
| --databases, -B | 导出几个数据库。参数后面所有名字参量都被看作数据库名。 |
| --default-character-set | 设置默认字符集，默认值为 utf8 |
| --help | 显示帮助信息并退出。 |
| --no-create-db, -n | 只导出数据，而不添加 CREATE DATABASE 语句。 |
| --no-create-info, -t | 只导出数据，而不添加 CREATE TABLE 语句。 |
| --no-data, -d | 不导出任何数据，只导出数据库表结构 |
| --tables | 覆盖--databases (-B)参数，指定需要导出的表名，在后面的版本会使用 table 取代 tables。 |
| --where, -w | 只转储给定的 WHERE 条件选择的记录。请注意如果条件包含命令解释符专用空格或字符，一定要将条件引用起来。 |


### 还原数据库
```
mysql>source filename
```

### 查看二进制文件
`mysqlbinlog mysqlbin-log.003209`

### 主要是查看从机
`show slave status \G;`

### 查看主机
`show master status;`
