---
ayout: post
title: 截取字符串去重后拼接
tags:
  - 语雀
categories:
  - - 语雀
    - 我的知识库
abbrlink: 23427b89
password: Grbk@2024
date: 2024-05-23 16:24:16
---
```
SELECT 
	(
	SELECT GROUP_CONCAT(DISTINCT SUBSTRING_INDEX(SUBSTRING_INDEX(a.path,',',b.help_topic_id + 1),',',-1)) as path
FROM  
(SELECT '土地决策系统,土地决策系统,省土地开发整治中心,矿管系统' as path FROM dual) a
JOIN 
mysql.help_topic b 
<!--more-->
ON b.help_topic_id < (LENGTH(a.path) - LENGTH(REPLACE(a.path,',','')) + 1)
	) path


```
```
CREATE DEFINER=`root`@`%` FUNCTION `GROUP_CONCAT_SUBSTRING_INDEX`(val VARCHAR(4000)) RETURNS varchar(4000) CHARSET utf8
BEGIN
	DECLARE result VARCHAR(4000) DEFAULT '';
SELECT 
	(
	SELECT GROUP_CONCAT(DISTINCT SUBSTRING_INDEX(SUBSTRING_INDEX(a.path,',',b.help_topic_id + 1),',',-1)) as path
FROM  
(SELECT val as path FROM dual) a
JOIN 
mysql.help_topic b 
ON b.help_topic_id < (LENGTH(a.path) - LENGTH(REPLACE(a.path,',','')) + 1)
	) path  INTO result;


	RETURN result;
END
```
```
调用	
SELECT GROUP_CONCAT_SUBSTRING_INDEX('土地决策系统,土地决策系统,省土地开发整治中心,矿管系统') from dual ;
```
