---
ayout: post
title: pg库语句
tags:
  - 语雀
categories:
  - - 语雀
    - 综合管理平台
abbrlink: 4dc60fa4
password: Grbk@2024
date: 2024-05-23 16:24:16
---
### 1、创建角色
```
CREATE ROLE "zhfwpt_zcyjs" CREATEDB LOGIN REPLICATION PASSWORD 'ZGPTzhs@1014';
```
### 2、创建模式
```
CREATE SCHEMA zhfwpt_zghs AUTHORIZATION zhfwpt_zghs;
```
<!--more-->

### 3、查询所有模式
```
select schema_name from information_schema.schemata;
```

### 4、查询所有角色
```
select rolname from pg_roles;
```

### 5、修改角色
```
ALTER ROLE "zhfwpt_yld" RENAME TO "zhfwpt_yld1";
```

### 6、修改模式
```
ALTER SCHEMA "zhfwpt_yld" RENAME TO "zhfwpt_yld1";
```

### 7、将模式下的所有表的增删改查授权给角色
```plsql
##所有表授权：
##shema说明：选择的模式为zhfwpt_cs，我将zhfwpt_cs模式下的所有表的增删改查授权给角色名为zhfwpt_cs的角色
grant select,insert,update,delete on all tables in schema "zhfwpt_cs" to zhfwpt_cs;


GRANT Connect, Create, Temporary ON DATABASE "zgpt_kjk_zwb" TO "zgpt_kjk_zhs";

GRANT Create, Usage ON SCHEMA "zgpt_kjk_zhs" TO "zgpt_kjk_zhs";

GRANT Delete, Insert, References, Select, Trigger, Truncate, Update ON TABLE "zgpt_kjk_zhs"."test4" TO "zgpt_kjk_zhs";

--grant select,insert,update,delete on all tables in schema "zgpt_kjk_zhs" to zgpt_kjk_zhs;

```
### 查询test用户table权限
```plsql
select table_name,table_schema,grantee,string_agg(privilege_type,',') from information_schema.table_privileges where grantee='test' group by table_name,table_schema,grantee;

```
### 查询test用户schema权限
```
select a.nspname,b.rolname,string_agg(a.pri_t,',') from (select nspname,(aclexplode(COALESCE(nspacl, acldefault('n'::"char",nspowner)))).grantee as grantee,(aclexplode(COALESCE(nspacl, acldefault('n'::"char",nspowner)))).privilege_type as pri_t from pg_namespace where nspname not like 'pg%' and nspname <> 'information_schema') a,pg_authid b where (a.grantee=b.oid or a.grantee=0) and b.rolname='test' group by a.nspname,b.rolname;

```
### 查询test用户[database](https://so.csdn.net/so/search?q=database&spm=1001.2101.3001.7020)权限
```plsql
select a.datname,b.rolname,string_agg(a.pri_t,',') from (select datname,(aclexplode(COALESCE(datacl, acldefault('d'::"char",datdba)))).grantee as grantee,(aclexplode(COALESCE(datacl, acldefault('d'::"char", datdba)))).privilege_type as pri_t from pg_database where datname not like 'template%') a,pg_roles b where (a.grantee=b.oid or a.grantee=0) and b.rolname='test' group by a.datname,b.rolname;

```
